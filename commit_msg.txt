fix: correcao do save de perfil e diagnostic de erros

- saveProfile agora sanitiza os updates para enviar apenas campos validos ao banco.
- Adicionados logs de sucesso e erro no console para facilitar diagnostico.
- ChannelSettings agora exibe toast de erro se o Supabase retornar falha.
- Corrigido problema silencioso que impedia o salvamento das configuracoes.

ACAO NECESSARIA:
Execute no SQL Editor do Supabase:
  ALTER TABLE profiles ADD COLUMN IF NOT EXISTS instagram_handle TEXT DEFAULT '';
